name: CI Notice

on:
  issues

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@main
      
      - uses: actions-cool/ci-notice@main
        with:
          ci: |
            npm install
            npm run package
          notice-types: 'dingding, issue'
          dingding-token: ${{ secrets.TEST_D }}
          lissue-labels: 'x1, x2'
          issue-assignees: 'xrkffgg, zoo-js-bot'
